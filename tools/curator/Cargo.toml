[package]
name = "curator"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ureq = "2.9.6"
route-recognizer = "0.3"
clap = { version = "4", features = ["derive"] }
